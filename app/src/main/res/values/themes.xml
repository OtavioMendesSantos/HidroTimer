<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema padrão simples e compatível com Wear OS -->
    <style name="Theme.HidroTimer" parent="@android:style/Theme.DeviceDefault">
        <item name="android:windowBackground">@android:color/black</item>
    </style>

</resources>
